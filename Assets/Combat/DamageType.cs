namespace Zombera.Combat
{
    /// <summary>
    /// Damage categories used for armor and resistance lookup.
    /// </summary>
    public enum DamageType
    {
        Generic    = 0,
        Melee      = 1,
        Ranged     = 2,
        Explosion  = 3,
        Fire       = 4,
    }
}
